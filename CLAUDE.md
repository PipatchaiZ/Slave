# CLAUDE.md

## Agent skills

### Issue tracker

Issues and PRDs live as local markdown files under `.scratch/<feature-slug>/` (no git remote). See `docs/agents/issue-tracker.md`.

### Triage labels

Uses the five canonical triage roles with their default strings. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: one `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
