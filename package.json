{
  "name": "slave-game",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p packages/engine && tsc -p apps/server && tsc -p apps/web",
    "dev": "concurrently -k -n server,web -c blue,magenta \"npm run dev -w @slave/server\" \"npm run dev -w @slave/web\"",
    "build": "npm run build -w @slave/web && npm run build -w @slave/server",
    "start": "npm run start -w @slave/server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
